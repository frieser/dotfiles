# PAM configuration for fingerprint ONLY
# This ensures we don't fall through to password automatically.
# Password auth is handled by switching to password.conf when user types.
auth required pam_fprintd.so
